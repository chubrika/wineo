# Backend API base used by /auth/google and API rewrites in development.
BACKEND_URL=http://localhost:4000

# Same value as wineo-back JWT_SECRET — used by middleware to verify the httpOnly session cookie.
# JWT_SECRET=your-super-secret-key-change-in-production
