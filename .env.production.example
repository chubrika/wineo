# Production — set these in Vercel (or copy to .env.production for local prod builds).
# Next.js rewrites /api/* to BACKEND_URL; set this to your API origin.
BACKEND_URL=https://your-backend.onrender.com
NEXT_PUBLIC_SITE_URL=https://wineo.ge
