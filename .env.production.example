# Production — set these in Vercel (Project → Settings → Environment Variables).
# REQUIRED: Backend API URL. /api/* requests are proxied here via next.config rewrites.
BACKEND_URL=https://your-backend.onrender.com
NEXT_PUBLIC_SITE_URL=https://wineo.ge
