# Development — copy to .env.development or rely on Next.js loading this as example.
# NEXT_PUBLIC_ vars are embedded at build time.
# BACKEND_URL defaults to http://localhost:4000 in next.config.ts if unset.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
