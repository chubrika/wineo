# Development — copy to .env.development (or rely on Next.js loading this as example)
# NEXT_PUBLIC_ vars are embedded at build time; use .env.development for local overrides.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
